"use strict";

const category = 'toys';

console.log(`https://someurl.com/${category}/5`);

const user = 'Nana';
alert(`Привет ${user}!`);

const isChecked = false,
      isClose = false;

console.log(isChecked || !isClose);